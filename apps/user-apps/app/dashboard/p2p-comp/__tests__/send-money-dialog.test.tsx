import { describe, it, vi, expect, Assertion} from "vitest";

describe("Send Money Dialog Component", () => {
  it("Send Money Dialog Render Check when isOpen is true", () => {
    
  });

  it("Send Money Dialog does not render when isOpen is false", () => {
    
  });
});
